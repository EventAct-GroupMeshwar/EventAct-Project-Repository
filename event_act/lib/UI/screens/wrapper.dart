import 'package:event_act/UI/screens/home/admin_home.dart';
import 'package:flutter/material.dart';

class Wrapper extends StatelessWidget {
  //import 'package:flutter/material.dart';

  const Wrapper({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AdminHome(
      
    );
  }
}